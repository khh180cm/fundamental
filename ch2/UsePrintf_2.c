#include <stdio.h>

int main(void) {
    // 서식문자의 삽입위치는 제한이 없기에 다양한 문자열을 조합해서 출력이 가능하다
    printf("My age: %d\n", 31);
    printf("%d is my point\n", 100);
    printf("Good\nMorning\nEverybody\n");
    return 0;
}
